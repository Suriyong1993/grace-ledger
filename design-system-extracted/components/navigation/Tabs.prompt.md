Segmented pill switcher for view filters (e.g. รายเดือน/รายปี, ทั้งหมด/รอดำเนินการ).

```jsx
<Tabs items={[{value:"month",label:"รายเดือน"},{value:"year",label:"รายปี"}]} value={range} onChange={setRange} />
```

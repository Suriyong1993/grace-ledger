Tabular-numeral money value with income/expense coloring. Use for every amount in tables, cards, and confirmations — never format currency inline by hand.

```jsx
<MoneyText value={5000} tone="income" />
<MoneyText value={1200} tone="expense" />
```

Amounts are never rounded for display — always 2 decimal places, real value.

KPI summary card — left accent strip instead of a gradient background ("financial terminal" style). The number is always the hero; no NumberTicker animation on money, ever.

```jsx
<StatCard label="รายรับเดือนนี้" value="฿245,000.00" tone="success" trend={12.4} hint="38 รายการ" />
```

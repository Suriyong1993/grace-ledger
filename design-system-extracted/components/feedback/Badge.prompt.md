Small pill label for tags/counts. `success`/`warning`/`destructive`/`info` map to the finance semantic colors — never repurpose green/red for anything other than credit/debit.

```jsx
<Badge variant="success">อนุมัติแล้ว</Badge>
<Badge variant="outline">Draft</Badge>
```

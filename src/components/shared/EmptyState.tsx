/**
 * @deprecated Import from "@/components/ui/empty-state" instead. This
 * re-export exists only until all 15 authenticated routes migrate to the
 * canonical ui/ primitive (Design System v3.0 Component Migration Tracking).
 */
export { EmptyState } from "@/components/ui/empty-state";
